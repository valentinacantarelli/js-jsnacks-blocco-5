// Scrivi una funzione che fonda due array (aventi lo stesso numero di
//     elementi) prendendo alternativamente gli elementi da uno e dall’altro
//     es. [a,b,c], [1,2,3] → [a,1,b,2,c,3].

function creaArrey(arg1, arg2){
    var nuovoArrey=[];
    for(var i=0;i<arg1.lenght;i++){
        nuovoArrey.push(lettere);
    }
    for (var i=0;i<arg2.lenght;i++){
        nuovoArray.push(numeri);
    }
}

var nuovoArrey=[];
var lettere=["a","b","c"];
var numeri=[1,2,3];


creaArrey(lettere,numeri);
console.log(nuovoArrey);